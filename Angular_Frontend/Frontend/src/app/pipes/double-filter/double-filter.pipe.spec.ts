import { DoubleFilterPipe } from './double-filter.pipe';

describe('DoubleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
