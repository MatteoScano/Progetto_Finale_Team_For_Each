/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ListFilteredByUserIdPipe } from './listFilteredByUserId.pipe';

describe('Pipe: ListFilteredByUserIde', () => {
  it('create an instance', () => {
    let pipe = new ListFilteredByUserIdPipe();
    expect(pipe).toBeTruthy();
  });
});
