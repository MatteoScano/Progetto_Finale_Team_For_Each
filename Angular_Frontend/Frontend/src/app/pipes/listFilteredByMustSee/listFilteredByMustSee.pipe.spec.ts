/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ListFilteredByMustSeePipe } from './listFilteredByMustSee.pipe';

describe('Pipe: ListFilteredByMustSeee', () => {
  it('create an instance', () => {
    let pipe = new ListFilteredByMustSeePipe();
    expect(pipe).toBeTruthy();
  });
});
