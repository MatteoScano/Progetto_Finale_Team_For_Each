import { GenrePipePipe } from './genre-pipe.pipe';

describe('GenrePipePipe', () => {
  it('create an instance', () => {
    const pipe = new GenrePipePipe();
    expect(pipe).toBeTruthy();
  });
});
